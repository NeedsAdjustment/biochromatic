{
  "name": "biochromatic",
  "version": "0.0.1",
  "dependencies": {
    "@astrojs/mdx": "^1.0.0",
    "@astrojs/rss": "^2.4.4",
    "@astrojs/sitemap": "^3.0.0",
    "astro": "^3.0.7",
    "pagefind": "^0.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && npm run postbuild",
    "postbuild": "pagefind --source dist/",
    "preview": "astro build && npm run postbuild && astro preview",
    "astro": "astro"
  },
  "type": "module",
  "devDependencies": {
    "prettier": "^3.0.2",
    "prettier-plugin-astro": "^0.11.1"
  }
}
